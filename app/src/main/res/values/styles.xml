<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    <style name="list_view" parent="@android:style/Widget.ListView">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:listSelector">@drawable/list_item_selector</item>
  		<item name="android:dividerHeight">5.0sp</item>
  		<item name="android:background">@color/second_grey</item>
    </style>
    
    <style name="article_title" parent="@android:style/Widget.TextView">
        <item name="android:textColor">#000</item>
    </style>
    
</resources>
